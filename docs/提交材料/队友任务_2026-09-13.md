# 队友任务包（2026-09-13 · VM 已启动）

> 郑子轩侧：编译已完成，**烧录卡在 USB 未进 VM**。请先把 DevKit 的 **CH343 透传给 Ubuntu**，或在 Windows 用 COM7 烧录。

共享盘：`/mnt/hgfs/VMware_share`（Win：`E:\openvela\contest2026_313_bianyuanxingzhe\VMware_share`）  
当前任务：`mailbox/CURRENT.json` → **TASK_024**，产物 `artifacts/nuttx_wifiui.bin`（1593428 B）

---

## 发给王筠昊（数据 · P0 收尾）

```
王筠昊你好，edge_walker 数据侧 P0 文档已经有了，请你用 Ubuntu 虚拟机 Cursor 做收尾（约 30 分钟）：

1. 打开仓库 ~/openvela/contest2026_313_bianyuanxingzhe
2. 阅读并确认 docs/提交材料/LD2451数据与策略复现.md 与 app/edge_walker/ew_ld2451.c 一致
3. 在 app/edge_walker 目录执行：
   gcc -O2 -Wall -Wextra -o /tmp/host_smoke host_smoke.c ew_ld2451.c && /tmp/host_smoke
   预期最后一行：ALL PASS
4. 若无代码改动：写回执 VMware_share/mailbox/guest_to_host/REPLY_DATA.md
   （写明：host_smoke 命令、输出 ALL PASS、文档路径）
5. 若需改 ew_ld2451.c：改完后 tr -d '\r' < VMware_share/scripts/run_ew_wifiui_build.sh 不必跑，
   只跑 host_smoke；回执里写 diff 摘要

不要改：alert_buzzer.c、ew_wifi_at.c、GPIO 脚位。
有问题读 VMware_share/AGENT_BUS.md，owner=guest 时才动编译。
```

---

## 发给韦政宇（Agent · P0 验收 + P1 可选）

```
韦政宇你好，ai_agent Skill 代码已在 app/edge_walker/（ew_agent*.c、skills/approach-warn、
ai_agent_ext/），郑子轩侧已编入 nuttx_wifiui.bin。请你用 VM Cursor 做赛题验收与署名回执：

【P0 · 必做 · 约 1 小时】
1. 仓库 ~/openvela/contest2026_313_bianyuanxingzhe，读 app/edge_walker/README.md § ai_agent Skill 演示
2. 确认以下文件存在且合理：
   - ew_agent.c / ew_agent_tool.c（Tool approach_alert → alert_output）
   - skills/approach-warn.md
   - alert_lcd.c 内 ew_agent_proactive_alert 调用
3. 等郑子轩烧录 nuttx_wifiui.bin 后，NSH（1Mbps COM7 或 VM /dev/ttyACM0）：
   ew fake 10 20
   预期日志含 [ew_agent] 与 [alert_output] level=...
4. 写回执：VMware_share/mailbox/guest_to_host/REPLY_AI_AGENT_SKILL.md
   （改了哪些文件、演示步骤、串口日志摘录）
5. 可选第二套：bash VMware_share/scripts/run_ew_agent_build.sh
   → artifacts/nuttx_ai_agent.bin（官方 ai_agent 进程栈，与 wifiui 二选一烧录）

【P1 · 有时间再做】
- board_overlay 或启动脚本把 MiSans-Regular.ttf 部署到 /data/font/
- 验证 Agent 页中文不再方框；写 REPLY_AGENT_UI.md

禁止：LLM 参与测距门限；erase_flash；WiFi 密码进 git/mailbox。
```

---

## 发给全员 · 当前阻塞（郑子轩操作）

1. VMware：**虚拟机 → 可移动设备 → 连接 CH343**（Guest 里应出现 `/dev/ttyACM0`）
2. Guest 自动烧录：`bash /mnt/hgfs/VMware_share/scripts/mailbox_exec_guest.sh`
3. 或 Windows COM7：`tools\sftool\sftool.exe` 烧 `VMware_share\artifacts\nuttx_wifiui.bin@0x12010000`
4. 屏上验收：预警 fake、WiFi 手动 SSID、Agent 快捷句

监听已可开：`bash /mnt/hgfs/VMware_share/scripts/start_guest_bus_loop.sh`
